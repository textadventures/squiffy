{
  "name": "squiffy",
  "private": true,
  "workspaces": [
    "compiler",
    "editor",
    "packager",
    "runtime",
    "site",
    "types"
  ],
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "version": "lerna version",
    "publish": "lerna publish from-package --no-private"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^9.0.3"
  }
}
