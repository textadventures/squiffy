{
  "name": "squiffy",
  "private": true,
  "workspaces": [
    "compiler",
    "editor",
    "packager",
    "runtime",
    "site",
    "types",
    "vite-plugin"
  ],
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "version": "lerna version --no-private",
    "publish": "lerna publish from-package --no-private"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.2.3"
  }
}
